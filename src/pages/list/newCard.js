import { Table, Modal, Button, Form, Input } from 'antd';
import React from 'react';
const FormItem = Form.Item;

export default ()=>{
    return (
    <div>
        <Table  />

        <Button>新建</Button>
        <Modal title="新建记录">
        ...
        </Modal>
    </div>
    )
}